//  App theme.

import SwiftUI

extension Color {
    /// betterAC's brand accent. Used as the app-wide tint and to mark servers
    /// that have a saved login.
    static let acAccent = Color(red: 0.60, green: 0.40, blue: 0.90)
}
